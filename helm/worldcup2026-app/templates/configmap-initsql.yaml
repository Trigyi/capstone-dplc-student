apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "worldcup2026-app.postgresFullname" . }}-initsql
  labels:
    {{- include "worldcup2026-app.labels" . | nindent 4 }}
data:
  init.sql: |-
{{ .Files.Get "files/init.sql" | indent 4 }}
